{
  "schema_version": 1,
  "lint": [
    ["docker", "compose", "config", "--quiet"]
  ],
  "tests": [
    ["docker", "compose", "config", "--quiet"]
  ],
  "build": {
    "commands": [
      ["docker", "compose", "config", "--quiet"]
    ],
    "outputs": ["docker-compose.yml", "COMPOSITION.md", "CONVENTIONS.md"]
  },
  "deploy": [
    ["docker", "compose", "up", "-d", "--wait"]
  ]
}
